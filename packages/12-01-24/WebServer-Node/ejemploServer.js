const http = require("http");

const server = http.createServer((req, res) => {
  // if(req.url=== '/'){
  //     res.writeHead(200,)
  // }
});

server.listen(8000, () => {
  console.log("clase de yo contigo");
});
